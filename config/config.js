require('dotenv').config();

/**
 * This file is intended to manage environment variables from the .env file.
 * Next.js has built-in support for environment variables, so this file may be redundant.
 * 
 * To use environment variables in Next.js:
 * 1. Prefix variables with NEXT_PUBLIC_ to expose them to the browser.
 * 2. Access them via `process.env.VARIABLE_NAME`.
 * 
 * Example:
 * const config = {
 *   apiKey: process.env.NEXT_PUBLIC_API_KEY,
 * };
 * 
 * module.exports = config;
 */

const config = {};

module.exports = config;
